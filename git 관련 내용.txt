GUI(gitHub)	탐색기
CLI(nodejs git)	CMD-dir
		PowerShell


cmd
node -v (node 버전 확인)
node ( node프로그램 열기 )
Ctrl+c 2번 ( node프로그램 닫기)

cmd 명령어
cd ( 경로 수정 명령어 )
1) 드라이브 이동
c:
d:

2) 경로이동
cd D:\RYC\MEVN

3) dir
해당 경로의 폴더 및 파일 확인

4) 경로 이동
cd.. ( 상위폴더 )
cd\ ( 최상단경로로 이동( c: , d: ))

npm init ( 초기화 )
npm i
npm install fxjs ( -g ) // -g 사용시 충돌이 일어나기 쉬움
npm uninstall fxjs ( -g )
npm list ( -g )


.gitignore 파일 내에는 push를 할 때 제외할 목록을 넣는다.

git init ( 초기화 ) 
git status ( 상태 확인 )
git add -A (.A) ( staging 하기(업로드 준비) / LF 이슈가 있어 CRLF로 바꿔주기 )
git commit -A
git log ( commit이 제대로 되었는지 확인 )
git remote add origin https://github.com/qazwsx5381/mevn.git
git push --set-upstream origin main // 안 될 경우 1)로

//트러블 슈팅!
1)안 될 경우 아이디와 메일을 다시 입력해야함
$ git config --global user.name "아이디"
$ git config --global user.email "이메일"

2) 제외한 파일이 남아있을 경우
git rm --cached ( node_modules ) 혹은 ( .gitignore ) 문제된 파일 / 폴더 넣기

